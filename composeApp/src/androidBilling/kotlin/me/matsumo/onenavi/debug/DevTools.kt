package me.matsumo.onenavi.debug

import android.content.Context

/**
 * デバッグ用ツールの初期化エントリポイント（billing ビルド版）。
 * billing ビルドでは何もしない。
 */
object DevTools {
    @Suppress("UNUSED_PARAMETER")
    fun initialize(context: Context) = Unit
}
